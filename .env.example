# Breach APIs (optional — the Breach Scout returns nothing without these)
HIBP_API_KEY=
INTELX_API_KEY=
# Tor SOCKS5 proxy (defaults: host "tor" under Docker, "127.0.0.1" locally)
TOR_SOCKS_HOST=tor
TOR_SOCKS_PORT=9050
# Research MCP adapter port
MCP_PORT=3200
